/*
 * Copyright (C) 2024-2025 Hideki Ikeda
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
package org.czeal.urireference

import com.fleeksoft.charset.Charsets
import org.czeal.urireference.TestUtils.assertThrowsISE
import org.czeal.urireference.TestUtils.assertThrowsNPE
import org.czeal.urireference.URIReference.Companion.parse
import kotlin.test.Test
import kotlin.test.assertEquals


class URIReferenceResolverTest {
    @Test
    fun test_resolve() {
        val uriRef1 = URIReferenceResolver().resolve(
            parse("g:h", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("g:h", uriRef1.toString())
        assertEquals(false, uriRef1.isRelativeReference)
        assertEquals("g", uriRef1.scheme)
        assertEquals(false, uriRef1.hasAuthority())
        assertEquals(null, uriRef1.authority)
        assertEquals(null, uriRef1.userinfo)
        assertEquals(null, uriRef1.host)
        assertEquals(-1, uriRef1.port)
        assertEquals("h", uriRef1.path)
        assertEquals(null, uriRef1.query)
        assertEquals(null, uriRef1.fragment)

        val uriRef2 = URIReferenceResolver().resolve(
            parse("g", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g", uriRef2.toString())
        assertEquals(false, uriRef2.isRelativeReference)
        assertEquals("http", uriRef2.scheme)
        assertEquals(true, uriRef2.hasAuthority())
        assertEquals("a", uriRef2.authority.toString())
        assertEquals(null, uriRef2.userinfo)
        assertEquals("a", uriRef2.host.toString())
        assertEquals("a", uriRef2.host!!.value)
        assertEquals(HostType.REGNAME, uriRef2.host!!.type)
        assertEquals(-1, uriRef2.port)
        assertEquals("/b/c/g", uriRef2.path)
        assertEquals(null, uriRef2.query)
        assertEquals(null, uriRef2.fragment)

        val uriRef3 = URIReferenceResolver().resolve(
            parse("./g", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g", uriRef3.toString())
        assertEquals(false, uriRef3.isRelativeReference)
        assertEquals("http", uriRef3.scheme)
        assertEquals(true, uriRef3.hasAuthority())
        assertEquals("a", uriRef3.authority.toString())
        assertEquals(null, uriRef3.userinfo)
        assertEquals("a", uriRef3.host.toString())
        assertEquals("a", uriRef3.host!!.value)
        assertEquals(HostType.REGNAME, uriRef3.host!!.type)
        assertEquals(-1, uriRef3.port)
        assertEquals("/b/c/g", uriRef3.path)
        assertEquals(null, uriRef3.query)
        assertEquals(null, uriRef3.fragment)

        val uriRef4 = URIReferenceResolver().resolve(
            parse("g/", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g/", uriRef4.toString())
        assertEquals(false, uriRef4.isRelativeReference)
        assertEquals("http", uriRef4.scheme)
        assertEquals(true, uriRef4.hasAuthority())
        assertEquals("a", uriRef4.authority.toString())
        assertEquals(null, uriRef4.userinfo)
        assertEquals("a", uriRef4.host.toString())
        assertEquals("a", uriRef4.host!!.value)
        assertEquals(HostType.REGNAME, uriRef4.host!!.type)
        assertEquals(-1, uriRef4.port)
        assertEquals("/b/c/g/", uriRef4.path)
        assertEquals(null, uriRef4.query)
        assertEquals(null, uriRef4.fragment)

        val uriRef5 = URIReferenceResolver().resolve(
            parse("/g", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/g", uriRef5.toString())
        assertEquals(false, uriRef5.isRelativeReference)
        assertEquals("http", uriRef5.scheme)
        assertEquals(true, uriRef4.hasAuthority())
        assertEquals("a", uriRef4.authority.toString())
        assertEquals(null, uriRef4.userinfo)
        assertEquals("a", uriRef4.host.toString())
        assertEquals("a", uriRef4.host!!.value)
        assertEquals(HostType.REGNAME, uriRef4.host!!.type)
        assertEquals(-1, uriRef4.port)
        assertEquals("/g", uriRef5.path)
        assertEquals(null, uriRef5.query)
        assertEquals(null, uriRef5.fragment)

        val uriRef6 = URIReferenceResolver().resolve(
            parse("//g", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://g", uriRef6.toString())
        assertEquals(false, uriRef6.isRelativeReference)
        assertEquals("http", uriRef6.scheme)
        assertEquals(true, uriRef6.hasAuthority())
        assertEquals("g", uriRef6.authority.toString())
        assertEquals(null, uriRef6.userinfo)
        assertEquals("g", uriRef6.host.toString())
        assertEquals("g", uriRef6.host!!.value)
        assertEquals(HostType.REGNAME, uriRef6.host!!.type)
        assertEquals(-1, uriRef6.port)
        assertEquals("", uriRef6.path)
        assertEquals(null, uriRef6.query)
        assertEquals(null, uriRef6.fragment)

        val uriRef7 = URIReferenceResolver().resolve(
            parse("?y", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/d;p?y", uriRef7.toString())
        assertEquals(false, uriRef7.isRelativeReference)
        assertEquals("http", uriRef7.scheme)
        assertEquals(true, uriRef7.hasAuthority())
        assertEquals("a", uriRef7.authority.toString())
        assertEquals(null, uriRef7.userinfo)
        assertEquals("a", uriRef7.host.toString())
        assertEquals("a", uriRef7.host!!.value)
        assertEquals(HostType.REGNAME, uriRef7.host!!.type)
        assertEquals(-1, uriRef7.port)
        assertEquals("/b/c/d;p", uriRef7.path)
        assertEquals("y", uriRef7.query)
        assertEquals(null, uriRef7.fragment)

        val uriRef8 = URIReferenceResolver().resolve(
            parse("g?y", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g?y", uriRef8.toString())
        assertEquals(false, uriRef8.isRelativeReference)
        assertEquals("http", uriRef8.scheme)
        assertEquals(true, uriRef8.hasAuthority())
        assertEquals("a", uriRef8.authority.toString())
        assertEquals(null, uriRef8.userinfo)
        assertEquals("a", uriRef8.host.toString())
        assertEquals("a", uriRef8.host!!.value)
        assertEquals(HostType.REGNAME, uriRef8.host!!.type)
        assertEquals(-1, uriRef8.port)
        assertEquals("/b/c/g", uriRef8.path)
        assertEquals("y", uriRef8.query)
        assertEquals(null, uriRef8.fragment)

        val uriRef9 = URIReferenceResolver().resolve(
            parse("#s", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/d;p?q#s", uriRef9.toString())
        assertEquals(false, uriRef9.isRelativeReference)
        assertEquals("http", uriRef9.scheme)
        assertEquals(true, uriRef9.hasAuthority())
        assertEquals("a", uriRef9.authority.toString())
        assertEquals(null, uriRef9.userinfo)
        assertEquals("a", uriRef9.host.toString())
        assertEquals("a", uriRef9.host!!.value)
        assertEquals(HostType.REGNAME, uriRef9.host!!.type)
        assertEquals(-1, uriRef9.port)
        assertEquals("/b/c/d;p", uriRef9.path)
        assertEquals("q", uriRef9.query)
        assertEquals("s", uriRef9.fragment)

        val uriRef10 = URIReferenceResolver().resolve(
            parse("g#s", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g#s", uriRef10.toString())
        assertEquals(false, uriRef10.isRelativeReference)
        assertEquals("http", uriRef10.scheme)
        assertEquals(true, uriRef10.hasAuthority())
        assertEquals("a", uriRef10.authority.toString())
        assertEquals(null, uriRef10.userinfo)
        assertEquals("a", uriRef10.host.toString())
        assertEquals("a", uriRef10.host!!.value)
        assertEquals(HostType.REGNAME, uriRef10.host!!.type)
        assertEquals(-1, uriRef10.port)
        assertEquals("/b/c/g", uriRef10.path)
        assertEquals(null, uriRef10.query)
        assertEquals("s", uriRef10.fragment)

        val uriRef11 = URIReferenceResolver().resolve(
            parse("g?y#s", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g?y#s", uriRef11.toString())
        assertEquals(false, uriRef11.isRelativeReference)
        assertEquals("http", uriRef11.scheme)
        assertEquals(true, uriRef11.hasAuthority())
        assertEquals("a", uriRef11.authority.toString())
        assertEquals(null, uriRef11.userinfo)
        assertEquals("a", uriRef11.host.toString())
        assertEquals("a", uriRef11.host!!.value)
        assertEquals(HostType.REGNAME, uriRef11.host!!.type)
        assertEquals(-1, uriRef11.port)
        assertEquals("/b/c/g", uriRef11.path)
        assertEquals("y", uriRef11.query)
        assertEquals("s", uriRef11.fragment)

        val uriRef12 = URIReferenceResolver().resolve(
            parse(";x", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/;x", uriRef12.toString())
        assertEquals(false, uriRef12.isRelativeReference)
        assertEquals("http", uriRef12.scheme)
        assertEquals(true, uriRef12.hasAuthority())
        assertEquals("a", uriRef12.authority.toString())
        assertEquals(null, uriRef12.userinfo)
        assertEquals("a", uriRef12.host.toString())
        assertEquals("a", uriRef12.host!!.value)
        assertEquals(HostType.REGNAME, uriRef12.host!!.type)
        assertEquals(-1, uriRef12.port)
        assertEquals("/b/c/;x", uriRef12.path)
        assertEquals(null, uriRef12.query)
        assertEquals(null, uriRef12.fragment)

        val uriRef13 = URIReferenceResolver().resolve(
            parse("g;x", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g;x", uriRef13.toString())
        assertEquals(false, uriRef13.isRelativeReference)
        assertEquals("http", uriRef13.scheme)
        assertEquals(true, uriRef13.hasAuthority())
        assertEquals("a", uriRef13.authority.toString())
        assertEquals(null, uriRef13.userinfo)
        assertEquals("a", uriRef13.host.toString())
        assertEquals("a", uriRef13.host!!.value)
        assertEquals(HostType.REGNAME, uriRef13.host!!.type)
        assertEquals(-1, uriRef13.port)
        assertEquals("/b/c/g;x", uriRef13.path)
        assertEquals(null, uriRef13.query)
        assertEquals(null, uriRef13.fragment)

        val uriRef14 = URIReferenceResolver().resolve(
            parse("g;x?y#s", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g;x?y#s", uriRef14.toString())
        assertEquals(false, uriRef14.isRelativeReference)
        assertEquals("http", uriRef14.scheme)
        assertEquals(true, uriRef14.hasAuthority())
        assertEquals("a", uriRef14.authority.toString())
        assertEquals(null, uriRef14.userinfo)
        assertEquals("a", uriRef14.host.toString())
        assertEquals("a", uriRef14.host!!.value)
        assertEquals(HostType.REGNAME, uriRef14.host!!.type)
        assertEquals(-1, uriRef14.port)
        assertEquals("/b/c/g;x", uriRef14.path)
        assertEquals("y", uriRef14.query)
        assertEquals("s", uriRef14.fragment)

        val uriRef15 = URIReferenceResolver().resolve(
            parse("", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/d;p?q", uriRef15.toString())
        assertEquals(false, uriRef15.isRelativeReference)
        assertEquals("http", uriRef15.scheme)
        assertEquals(true, uriRef15.hasAuthority())
        assertEquals("a", uriRef15.authority.toString())
        assertEquals(null, uriRef15.userinfo)
        assertEquals("a", uriRef15.host.toString())
        assertEquals("a", uriRef15.host!!.value)
        assertEquals(HostType.REGNAME, uriRef15.host!!.type)
        assertEquals(-1, uriRef15.port)
        assertEquals("/b/c/d;p", uriRef15.path)
        assertEquals("q", uriRef15.query)
        assertEquals(null, uriRef15.fragment)

        val uriRef16 = URIReferenceResolver().resolve(
            parse(".", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/", uriRef16.toString())
        assertEquals(false, uriRef16.isRelativeReference)
        assertEquals("http", uriRef16.scheme)
        assertEquals(true, uriRef16.hasAuthority())
        assertEquals("a", uriRef16.authority.toString())
        assertEquals(null, uriRef16.userinfo)
        assertEquals("a", uriRef16.host.toString())
        assertEquals("a", uriRef16.host!!.value)
        assertEquals(HostType.REGNAME, uriRef16.host!!.type)
        assertEquals(-1, uriRef16.port)
        assertEquals("/b/c/", uriRef16.path)
        assertEquals(null, uriRef16.query)
        assertEquals(null, uriRef16.fragment)

        val uriRef17 = URIReferenceResolver().resolve(
            parse("./", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/", uriRef17.toString())
        assertEquals(false, uriRef17.isRelativeReference)
        assertEquals("http", uriRef17.scheme)
        assertEquals(true, uriRef17.hasAuthority())
        assertEquals("a", uriRef17.authority.toString())
        assertEquals(null, uriRef17.userinfo)
        assertEquals("a", uriRef17.host.toString())
        assertEquals("a", uriRef17.host!!.value)
        assertEquals(HostType.REGNAME, uriRef17.host!!.type)
        assertEquals(-1, uriRef17.port)
        assertEquals("/b/c/", uriRef17.path)
        assertEquals(null, uriRef17.query)
        assertEquals(null, uriRef17.fragment)

        val uriRef18 = URIReferenceResolver().resolve(
            parse("..", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/", uriRef18.toString())
        assertEquals(false, uriRef18.isRelativeReference)
        assertEquals("http", uriRef18.scheme)
        assertEquals(true, uriRef18.hasAuthority())
        assertEquals("a", uriRef18.authority.toString())
        assertEquals(null, uriRef18.userinfo)
        assertEquals("a", uriRef18.host.toString())
        assertEquals("a", uriRef18.host!!.value)
        assertEquals(HostType.REGNAME, uriRef18.host!!.type)
        assertEquals(-1, uriRef18.port)
        assertEquals("/b/", uriRef18.path)
        assertEquals(null, uriRef18.query)
        assertEquals(null, uriRef18.fragment)

        val uriRef19 = URIReferenceResolver().resolve(
            parse("../", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/", uriRef19.toString())
        assertEquals(false, uriRef19.isRelativeReference)
        assertEquals("http", uriRef19.scheme)
        assertEquals(true, uriRef19.hasAuthority())
        assertEquals("a", uriRef19.authority.toString())
        assertEquals(null, uriRef19.userinfo)
        assertEquals("a", uriRef19.host.toString())
        assertEquals("a", uriRef19.host!!.value)
        assertEquals(HostType.REGNAME, uriRef19.host!!.type)
        assertEquals(-1, uriRef19.port)
        assertEquals("/b/", uriRef19.path)
        assertEquals(null, uriRef19.query)
        assertEquals(null, uriRef19.fragment)

        val uriRef20 = URIReferenceResolver().resolve(
            parse("../g", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/g", uriRef20.toString())
        assertEquals(false, uriRef20.isRelativeReference)
        assertEquals("http", uriRef20.scheme)
        assertEquals(true, uriRef20.hasAuthority())
        assertEquals("a", uriRef20.authority.toString())
        assertEquals(null, uriRef20.userinfo)
        assertEquals("a", uriRef20.host.toString())
        assertEquals("a", uriRef20.host!!.value)
        assertEquals(HostType.REGNAME, uriRef20.host!!.type)
        assertEquals(-1, uriRef20.port)
        assertEquals("/b/g", uriRef20.path)
        assertEquals(null, uriRef20.query)
        assertEquals(null, uriRef20.fragment)

        val uriRef21 = URIReferenceResolver().resolve(
            parse("../..", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/", uriRef21.toString())
        assertEquals(false, uriRef21.isRelativeReference)
        assertEquals("http", uriRef21.scheme)
        assertEquals(true, uriRef21.hasAuthority())
        assertEquals("a", uriRef21.authority.toString())
        assertEquals(null, uriRef21.userinfo)
        assertEquals("a", uriRef21.host.toString())
        assertEquals("a", uriRef21.host!!.value)
        assertEquals(HostType.REGNAME, uriRef21.host!!.type)
        assertEquals(-1, uriRef21.port)
        assertEquals("/", uriRef21.path)
        assertEquals(null, uriRef21.query)
        assertEquals(null, uriRef21.fragment)

        val uriRef22 = URIReferenceResolver().resolve(
            parse("../../", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/", uriRef22.toString())
        assertEquals(false, uriRef22.isRelativeReference)
        assertEquals("http", uriRef22.scheme)
        assertEquals(true, uriRef22.hasAuthority())
        assertEquals("a", uriRef22.authority.toString())
        assertEquals(null, uriRef22.userinfo)
        assertEquals("a", uriRef22.host.toString())
        assertEquals("a", uriRef22.host!!.value)
        assertEquals(HostType.REGNAME, uriRef22.host!!.type)
        assertEquals(-1, uriRef22.port)
        assertEquals("/", uriRef22.path)
        assertEquals(null, uriRef22.query)
        assertEquals(null, uriRef22.fragment)

        val uriRef23 = URIReferenceResolver().resolve(
            parse("../../g", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/g", uriRef23.toString())
        assertEquals(false, uriRef23.isRelativeReference)
        assertEquals("http", uriRef23.scheme)
        assertEquals(true, uriRef23.hasAuthority())
        assertEquals("a", uriRef23.authority.toString())
        assertEquals(null, uriRef23.userinfo)
        assertEquals("a", uriRef23.host.toString())
        assertEquals("a", uriRef23.host!!.value)
        assertEquals(HostType.REGNAME, uriRef23.host!!.type)
        assertEquals(-1, uriRef23.port)
        assertEquals("/g", uriRef23.path)
        assertEquals(null, uriRef23.query)
        assertEquals(null, uriRef23.fragment)

        val uriRef24 = URIReferenceResolver().resolve(
            parse("g?y/./x", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g?y/./x", uriRef24.toString())
        assertEquals(false, uriRef24.isRelativeReference)
        assertEquals("http", uriRef24.scheme)
        assertEquals(true, uriRef24.hasAuthority())
        assertEquals("a", uriRef24.authority.toString())
        assertEquals(null, uriRef24.userinfo)
        assertEquals("a", uriRef24.host.toString())
        assertEquals("a", uriRef24.host!!.value)
        assertEquals(HostType.REGNAME, uriRef24.host!!.type)
        assertEquals(-1, uriRef24.port)
        assertEquals("/b/c/g", uriRef24.path)
        assertEquals("y/./x", uriRef24.query)
        assertEquals(null, uriRef24.fragment)

        val uriRef25 = URIReferenceResolver().resolve(
            parse("g?y/../x", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g?y/../x", uriRef25.toString())
        assertEquals(false, uriRef25.isRelativeReference)
        assertEquals("http", uriRef25.scheme)
        assertEquals(true, uriRef25.hasAuthority())
        assertEquals("a", uriRef25.authority.toString())
        assertEquals(null, uriRef25.userinfo)
        assertEquals("a", uriRef25.host.toString())
        assertEquals("a", uriRef25.host!!.value)
        assertEquals(HostType.REGNAME, uriRef25.host!!.type)
        assertEquals(-1, uriRef25.port)
        assertEquals("/b/c/g", uriRef25.path)
        assertEquals("y/../x", uriRef25.query)
        assertEquals(null, uriRef25.fragment)

        val uriRef26 = URIReferenceResolver().resolve(
            parse("g#s/./x", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g#s/./x", uriRef26.toString())
        assertEquals(false, uriRef26.isRelativeReference)
        assertEquals("http", uriRef26.scheme)
        assertEquals(true, uriRef26.hasAuthority())
        assertEquals("a", uriRef26.authority.toString())
        assertEquals(null, uriRef26.userinfo)
        assertEquals("a", uriRef26.host.toString())
        assertEquals("a", uriRef26.host!!.value)
        assertEquals(HostType.REGNAME, uriRef26.host!!.type)
        assertEquals(-1, uriRef26.port)
        assertEquals("/b/c/g", uriRef26.path)
        assertEquals(null, uriRef26.query)
        assertEquals("s/./x", uriRef26.fragment)

        val uriRef27 = URIReferenceResolver().resolve(
            parse("g#s/../x", Charsets.UTF8), parse("http://a/b/c/d;p?q", Charsets.UTF8)
        )
        assertEquals("http://a/b/c/g#s/../x", uriRef27.toString())
        assertEquals(false, uriRef27.isRelativeReference)
        assertEquals("http", uriRef27.scheme)
        assertEquals(true, uriRef27.hasAuthority())
        assertEquals("a", uriRef27.authority.toString())
        assertEquals(null, uriRef27.userinfo)
        assertEquals("a", uriRef27.host.toString())
        assertEquals("a", uriRef27.host!!.value)
        assertEquals(HostType.REGNAME, uriRef27.host!!.type)
        assertEquals(-1, uriRef27.port)
        assertEquals("/b/c/g", uriRef27.path)
        assertEquals(null, uriRef27.query)
        assertEquals("s/../x", uriRef27.fragment)

        assertThrowsNPE<Throwable>(
            "The input string must not be null.",
            {
                URIReferenceResolver().resolve(
                    parse(null as String?, Charsets.UTF8),
                    parse("http://a/b/c/d;p?q", Charsets.UTF8)
                )
            })

        assertThrowsISE<Throwable>(
            "The base URI must have a scheme.",
            {
                URIReferenceResolver().resolve(
                    parse("g", Charsets.UTF8), parse("/a/b/c/d;p?q", Charsets.UTF8)
                )
            })

        assertThrowsISE<Throwable>(
            "The base URI must not have a fragment.",
            {
                URIReferenceResolver().resolve(
                    parse("g", Charsets.UTF8), parse("http://a/b/c/d;p?q#s", Charsets.UTF8)
                )
            })
    }
}
